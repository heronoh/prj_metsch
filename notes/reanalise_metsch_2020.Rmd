---
title: "reanalise_metsch_2020"
author: "Heron"
date: "5/4/2020"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

#Metschnikowia genus reanalyses after PhD graduation

#Add new species


#Run BUSCO4 for all genomes

```{bash}
#docker run --rm -e USERID=1007 -u 1007 -v /home/paula/projeto_xylose:/home/paula/projeto_xylose -w /home/paula/projeto_xylose ezlabgva/busco:v4.0.5_cv1 busco -i genomas/as_gos.fna -o busco_as_gos -l saccharomycetes_odb10 -m geno -c 23;
```


```{R}
files <- list.files(genomes_dir)
saplpy(files, paste0(genomes_dir,files))

function(geno_ctgs)
```















## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
